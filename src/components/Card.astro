---
const { image, title, text, variant = "primary" } = Astro.props;
---


<div class={`card ${variant}`}>
  <img src={image} alt={title} />
  <h3>{title}</h3>
  <p>{text}</p>
</div>

<style>
  .card {
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  border-radius: 12px;
  padding: 16px;
}

.card img {
  max-width: 100%;
  height: auto;
  margin-bottom: 12px;
}

.card.primary h3 {
  font-family: "Inter", sans-serif;
  font-size: 1.5rem;
  font-weight: normal;
}

.card.secondary p {
  font-family: "Anton", sans-serif;
  font-size: 3rem;
  font-weight: 400;
}

.card p {
  margin: 0;
  font-size: 0.95rem;
  font-family: "Inter", sans-serif;
  font-weight: normal;
}
</style>